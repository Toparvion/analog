spring:
  application:
    name: AnaLog
    admin:
      enabled: true

server:
  session:
    persistent: true
    cookie:
      max-age: 86400

logging:
  level:
    root: info
    ru.ftc.upc.testing.analog: debug
    ru.ftc.upc.testing.analog.service.EncodingDetector: trace